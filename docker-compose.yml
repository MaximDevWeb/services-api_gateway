version: "3.5"

services:
  node:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    container_name: api_gateway
    ports:
      - 3010:4000
    volumes:
      - ./site:/user/www/site
    networks:
      - gateway
networks:
  gateway:
    name: microservices